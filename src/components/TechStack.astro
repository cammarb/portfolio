---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"button">;

const { class: className, ...props } = Astro.props;
---

<a
  class:list={[
    className,
    "px-3 py-2 rounded flex bg-emerald-100 hover:bg-emerald-200",
  ]}
  {...props}
>
  <slot />
  </a>
