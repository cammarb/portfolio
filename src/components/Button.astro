---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"button">;

const { class: className, ...props } = Astro.props;
---

<button
  class:list={[
    className,
    "px-2 py-1 rounded text-slate-50 bg-emerald-400 hover:bg-emerald-500",
  ]}
  {...props}
>
  <slot />
</button>
